---
phase: 04-results-ui-and-frontend-integration
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - frontend/src/pages/HistoryPage.tsx
  - frontend/src/hooks/useHistory.ts
  - frontend/src/lib/pdf.ts
  - frontend/src/pages/ResultsPage.tsx
autonomous: false
requirements: [HIST-01, HIST-02, EXPT-01]

must_haves:
  truths:
    - "History page shows a paginated table of all past analyses with filename, date, verdict badge, confidence, status, truncated hash, and processing time"
    - "User can search history by filename and filter by verdict (REAL/FAKE/failed)"
    - "Clicking a history row expands inline to show verdict badge, confidence bar, visual/audio scores, truncated hash, and View full results link"
    - "User can paginate through history with 10 items per page"
    - "Empty state shows friendly icon, 'No analyses yet' message, and CTA to /upload"
    - "User can download a PDF verification report from both results page and history row"
    - "PDF contains TrustChain-AV branding, verdict, confidence, scores, hash, filename, date, processing time, decision basis, blockchain placeholder"
  artifacts:
    - path: "frontend/src/pages/HistoryPage.tsx"
      provides: "Full history table with search, filter, pagination, expandable rows, PDF download"
      min_lines: 150
    - path: "frontend/src/hooks/useHistory.ts"
      provides: "Custom hook for fetching paginated history with search/filter state"
      exports: ["useHistory"]
    - path: "frontend/src/lib/pdf.ts"
      provides: "PDF report generation utility using jsPDF"
      exports: ["generateReport"]
  key_links:
    - from: "frontend/src/hooks/useHistory.ts"
      to: "/api/uploads"
      via: "getUploads from lib/api.ts"
      pattern: "getUploads"
    - from: "frontend/src/pages/HistoryPage.tsx"
      to: "frontend/src/hooks/useHistory.ts"
      via: "useHistory hook"
      pattern: "useHistory"
    - from: "frontend/src/lib/pdf.ts"
      to: "jspdf"
      via: "dynamic import"
      pattern: "import.*jspdf"
    - from: "frontend/src/pages/ResultsPage.tsx"
      to: "frontend/src/lib/pdf.ts"
      via: "generateReport call on Download PDF click"
      pattern: "generateReport"
---

<objective>
History page with paginated, searchable, filterable table with expandable rows, plus PDF verification report generation.

Purpose: Deliver the history browsing experience (search, filter, expand, navigate to details) and the PDF export capability, completing the detection-facing user experience for Phase 4.

Output: Working HistoryPage with full table, useHistory hook, PDF generation utility, and wired Download PDF buttons on both results and history pages.
</objective>

<execution_context>
@C:/Users/zopev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/zopev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-results-ui-and-frontend-integration/04-CONTEXT.md
@.planning/phases/04-results-ui-and-frontend-integration/04-RESEARCH.md
@.planning/phases/04-results-ui-and-frontend-integration/04-01-SUMMARY.md

@frontend/src/lib/api.ts
@frontend/src/pages/HistoryPage.tsx
@frontend/src/pages/ResultsPage.tsx
@frontend/src/hooks/useJobStatus.ts

<interfaces>
<!-- From Plan 01 outputs -- these will exist when Plan 02 executes -->

From frontend/src/lib/api.ts (after Plan 01):
```typescript
export interface UploadStatusResponse {
  id: string; filename: string; status: string; error_message: string | null;
  created_at: string; verdict: string | null; confidence: number | null;
  visual_score: number | null; audio_score: number | null;
  speech_detected: boolean | null; audio_weight: number | null;
  file_hash: string | null; processing_time: number | null;
  completed_at: string | null;
}

export interface UploadListResponse {
  items: UploadStatusResponse[];
  total: number;
  page: number;
  per_page: number;
  pages: number;
}

export async function getUploads(params: {
  page?: number; per_page?: number; search?: string; verdict?: string;
}): Promise<UploadListResponse>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install jsPDF, create PDF utility, useHistory hook, and full HistoryPage</name>
  <files>frontend/src/lib/pdf.ts, frontend/src/hooks/useHistory.ts, frontend/src/pages/HistoryPage.tsx</files>
  <action>
    Step 1: Install jsPDF in the frontend directory:
    ```bash
    cd frontend && npm install jspdf
    ```

    Step 2: Create frontend/src/lib/pdf.ts -- shared PDF generation utility.
    - Export async function `generateReport(data: UploadStatusResponse): Promise<void>`
    - Use dynamic import: `const { jsPDF } = await import("jspdf")`
    - Create a new jsPDF document (default A4 portrait)
    - Layout:
      - Header: "TrustChain-AV" in 22pt bold, "Verification Report" in 14pt below
      - Horizontal divider line
      - Report fields as label: value pairs (11pt, bold label, normal value):
        - Generated: current date/time (new Date().toLocaleString())
        - Filename: data.filename
        - Date Submitted: new Date(data.created_at).toLocaleString()
        - Verdict: data.verdict or "N/A"
        - Confidence: Math.round((data.confidence ?? 0) * 100) + "%" or "N/A" if null
        - Visual Score: Math.round((data.visual_score ?? 0) * 100) + "%" or "N/A" if null
        - Audio Score: (data.audio_score !== null && data.audio_score > 0) ? Math.round(data.audio_score * 100) + "%" : "N/A"
        - Speech Detected: data.speech_detected ? "Yes" : "No"
        - SHA-256 Hash: data.file_hash or "N/A"
        - Processing Time: data.processing_time ? data.processing_time.toFixed(1) + "s" : "N/A"
      - Decision basis in italic 9pt: "Prototype model -- verdict is based on untrained fusion weights"
      - Blockchain section heading in bold 11pt: "Blockchain Verification"
      - Below: "Not yet registered. Blockchain registration available in a future update." in normal 10pt
    - Save as: `trustchain-report-${data.id}.pdf`
    - Import type only: `import type { UploadStatusResponse } from "./api"`

    Step 3: Create frontend/src/hooks/useHistory.ts -- custom hook for history data.
    - Export function `useHistory(page: number, search: string, filter: string)`
    - Uses useState for data (UploadListResponse | null) and loading (boolean)
    - Uses useEffect triggered by [page, search, filter] to call getUploads from lib/api.ts
    - Build params object: { page, per_page: 10 }, conditionally add search if non-empty, add verdict: filter if filter !== "all"
    - Set loading true before fetch, false after (in finally)
    - Return { data, loading }

    Step 4: Replace frontend/src/pages/HistoryPage.tsx placeholder with full implementation.
    - State: page (number, starts at 1), search (string), filter (string, "all" | "REAL" | "FAKE" | "failed"), expandedIds (Set<string>), searchInput (string for debounced input)
    - Use the useHistory hook with (page, search, filter)
    - Search debounce: useState for searchInput, useEffect with 300ms setTimeout to update actual search state. Reset page to 1 when search or filter changes.
    - Layout structure:
      a. Header row: "Analysis History" title
      b. Controls row: Search input (text input with placeholder "Search by filename...", w-full md:w-72) on left, filter dropdown (select element with options: All, Real, Fake, Failed) on right. Use standard HTML input/select styled with Tailwind (rounded-md border border-zinc-300 px-3 py-2 text-sm).
      c. Table: HTML table with Tailwind classes (w-full text-sm). No shadcn Table needed -- use plain `<table>` with appropriate styling.
         - Columns: Filename, Submitted, Verdict, Confidence, Status, Hash, Time
         - Each row is clickable (cursor-pointer, hover:bg-zinc-50). onClick toggles expandedIds Set.
         - Verdict column: colored badge span (green bg for REAL, red bg for FAKE, zinc bg for others)
         - Confidence: Math.round(confidence * 100) + "%" or "--"
         - Status: capitalize, colored text (green for completed, red for failed, indigo for in-progress)
         - Hash: first 8 chars of file_hash + "..." or "--"
         - Time: processing_time?.toFixed(1) + "s" or "--"
         - Expanded row: when item.id is in expandedIds, render a `<tr>` below with a single `<td colSpan={7}>` containing:
           - Verdict badge (larger), confidence bar (same style as ResultsPage), visual score, audio score, truncated hash (first 16 chars)
           - "View full results" Link to `/results/${item.id}` styled as text-indigo-600 hover:underline
           - "Download PDF" button (calls generateReport(item) on click) styled as text-sm text-zinc-600 hover:text-zinc-900
      d. Empty state: when data exists but items is empty, show centered content: FileSearch icon from lucide-react at 64px (h-16 w-16 text-zinc-300), "No analyses yet" in text-lg font-medium text-zinc-500, "Upload a video to get started" in text-sm text-zinc-400, and a Link to /upload styled as CTA button.
      e. Pagination: when data exists and pages > 1, show a flex row at bottom: "Page X of Y" text, Previous button (disabled if page===1), Next button (disabled if page===data.pages). Buttons styled with border border-zinc-300 rounded-md px-3 py-1.5 text-sm.
      f. Loading state: while loading is true, show a Loader2 animate-spin centered.

    Icons to import from lucide-react: FileSearch, Loader2, ChevronDown, ChevronUp, Eye, Ear, FileDown.
    Import Link from react-router-dom.
    Import generateReport from lib/pdf.ts.
    Import useHistory from hooks/useHistory.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>HistoryPage shows paginated table with search, filter, expandable rows with detail view and PDF download, empty state with CTA. useHistory hook fetches data. PDF utility generates downloadable report. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Download PDF button on ResultsPage</name>
  <files>frontend/src/pages/ResultsPage.tsx</files>
  <action>
    Update ResultsPage.tsx to wire the "Download PDF" button that was added as a disabled placeholder in Plan 01 Task 2.

    1. Import generateReport from "../lib/pdf" (import the function, not type-only)
    2. Import FileDown from lucide-react (if not already imported)
    3. Find the "Download PDF" button in the post-result actions section
    4. Remove the disabled attribute and "Coming soon" title
    5. Add onClick handler that calls `generateReport(status)` where status is the UploadStatusResponse from useJobStatus
    6. Add loading state: useState for pdfLoading (boolean). Set true before generateReport, false after (in finally block). Show Loader2 spinner instead of FileDown icon while loading.
    7. Button text: "Download PDF" (or "Generating..." while loading)

    The button should be styled consistently with the other action buttons in the row:
    - Use a border border-zinc-300 rounded-md px-4 py-2 text-sm font-medium text-zinc-700 hover:bg-zinc-50 style (secondary button style)
    - Icon + text: FileDown icon h-4 w-4 inline with gap-2
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>Download PDF button on ResultsPage is functional -- clicking it generates and downloads a PDF report for the current analysis. Button shows loading state during generation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 4 UI: multi-card results page with PDF download, paginated/searchable history table with expandable rows, and HomePage with quick stats</what-built>
  <how-to-verify>
    1. Start the application: `docker compose up`
    2. Visit http://localhost:5173/ -- verify quick stats cards (total, real, fake counts) and CTA buttons
    3. Upload a video via /upload and wait for analysis to complete
    4. On the results page (/results/{id}):
       a. Verify 3-card layout: verdict card, modality breakdown card (with decision basis text), metadata card (with processing time)
       b. Click "Share results link" -- verify URL copied and button text changes to "Copied!"
       c. Click "Download PDF" -- verify a PDF file downloads with all fields (verdict, confidence, scores, hash, filename, date, processing time, blockchain placeholder)
       d. Click "Upload another video" -- verify navigation to /upload
    5. Visit /history:
       a. Verify table shows the analysis with all columns (filename, date, verdict badge, confidence, status, hash, processing time)
       b. Click a row -- verify it expands showing detailed scores, confidence bar, "View full results" link, and "Download PDF" button
       c. Click "View full results" in expanded row -- verify navigation to /results/{id}
       d. Click "Download PDF" in expanded row -- verify PDF downloads
       e. Test search: type part of filename -- verify table filters
       f. Test filter dropdown: select "Real" or "Fake" -- verify table filters
       g. If no analyses exist, verify empty state with icon, message, and CTA to /upload
    6. Visit / again -- verify stats updated after new analysis
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `cd backend && python -m pytest tests/ -v` -- all backend tests pass
2. `cd frontend && npx tsc --noEmit` -- frontend compiles without type errors
3. Manual: Full user flow from upload through results (multi-card + PDF download) to history (search, filter, expand, PDF)
4. Manual: Empty state on history page when no analyses exist
5. Manual: PDF opens correctly with all required fields
</verification>

<success_criteria>
- History page shows paginated table of past analyses with all specified columns
- Search by filename filters results, filter by verdict (REAL/FAKE/failed) works
- Clicking a row expands inline with detail view including "View full results" link
- Empty state shows friendly message and CTA when no analyses exist
- PDF downloads from both results page and history expanded rows
- PDF contains: TrustChain-AV branding, verdict, confidence, visual/audio scores, speech detected, SHA-256 hash, filename, date, processing time, decision basis text, blockchain placeholder section
- All backend tests pass, frontend TypeScript compiles
</success_criteria>

<output>
After completion, create `.planning/phases/04-results-ui-and-frontend-integration/04-02-SUMMARY.md`
</output>
