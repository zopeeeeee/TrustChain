---
phase: 04-results-ui-and-frontend-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/uploads.py
  - backend/app/schemas/upload.py
  - backend/tests/test_uploads.py
  - frontend/src/lib/api.ts
  - frontend/src/pages/ResultsPage.tsx
  - frontend/src/pages/HomePage.tsx
autonomous: true
requirements: [RSLT-01, RSLT-02, HIST-01]

must_haves:
  truths:
    - "Results page displays verdict in a dedicated card with color-coded confidence bar"
    - "Results page shows modality breakdown card with visual score, audio score, speech info, and decision basis text"
    - "Results page shows metadata card with filename, submission date, processing time, and SHA-256 hash"
    - "Results page has post-result action buttons: Upload another video, Share results link, Download PDF placeholder"
    - "Home page shows quick stats: total analyses, real count, fake count, with links to upload and history"
    - "Backend GET /api/uploads returns paginated list with search and filter support"
    - "Backend GET /api/uploads/stats returns aggregate counts"
    - "processing_time is computed and returned in status and list responses"
  artifacts:
    - path: "backend/app/api/uploads.py"
      provides: "GET /api/uploads list endpoint and GET /api/uploads/stats endpoint"
      exports: ["list_uploads", "get_stats"]
    - path: "backend/app/schemas/upload.py"
      provides: "UploadListResponse and StatsResponse schemas, processing_time field"
      contains: "UploadListResponse"
    - path: "frontend/src/pages/ResultsPage.tsx"
      provides: "Multi-card results layout with verdict, modality, metadata cards and post-result actions"
      min_lines: 100
    - path: "frontend/src/pages/HomePage.tsx"
      provides: "Quick stats dashboard with total analyses, verdict breakdown, and navigation links"
      min_lines: 40
  key_links:
    - from: "frontend/src/pages/HomePage.tsx"
      to: "/api/uploads/stats"
      via: "fetch in useEffect"
      pattern: "fetch.*api/uploads/stats"
    - from: "backend/app/api/uploads.py"
      to: "Upload model"
      via: "SQLAlchemy query with pagination, search, filter"
      pattern: "select\\(Upload\\)"
---

<objective>
Backend list/stats endpoints and Results page multi-card refactor with HomePage quick stats.

Purpose: Provide the data layer (paginated list endpoint, stats endpoint, processing_time computation) that both the results page and history page depend on, plus refactor ResultsPage into the user-decided multi-card layout and enhance HomePage with quick stats.

Output: Working GET /api/uploads and GET /api/uploads/stats backend endpoints with tests; ResultsPage with 3-card layout (verdict, modality, metadata) and post-result actions; HomePage with quick stats dashboard.
</objective>

<execution_context>
@C:/Users/zopev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/zopev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-results-ui-and-frontend-integration/04-CONTEXT.md
@.planning/phases/04-results-ui-and-frontend-integration/04-RESEARCH.md

@backend/app/api/uploads.py
@backend/app/schemas/upload.py
@backend/app/models/upload.py
@backend/tests/test_uploads.py
@frontend/src/lib/api.ts
@frontend/src/pages/ResultsPage.tsx
@frontend/src/pages/HomePage.tsx

<interfaces>
<!-- Key types and contracts the executor needs -->

From backend/app/models/upload.py:
```python
class Upload(Base):
    __tablename__ = "uploads"
    id: Mapped[uuid.UUID]
    filename: Mapped[str]
    file_path: Mapped[str | None]
    file_hash: Mapped[str | None]
    status: Mapped[str]
    verdict: Mapped[str | None]
    confidence: Mapped[float | None]
    visual_score: Mapped[float | None]
    audio_score: Mapped[float | None]
    speech_detected: Mapped[bool | None]
    audio_weight: Mapped[float | None]
    created_at: Mapped[datetime]
    error_message: Mapped[str | None]
    completed_at: Mapped[datetime | None]
```

From backend/app/schemas/upload.py:
```python
class UploadStatusResponse(BaseModel):
    id: str
    filename: str
    status: str
    error_message: str | None = None
    created_at: str
    verdict: str | None = None
    confidence: float | None = None
    visual_score: float | None = None
    audio_score: float | None = None
    speech_detected: bool | None = None
    audio_weight: float | None = None
    file_hash: str | None = None
```

From frontend/src/lib/api.ts:
```typescript
export interface UploadStatusResponse {
  id: string; filename: string; status: string; error_message: string | null;
  created_at: string; verdict: string | null; confidence: number | null;
  visual_score: number | null; audio_score: number | null;
  speech_detected: boolean | null; audio_weight: number | null;
  file_hash: string | null;
}
export async function getUploadStatus(id: string): Promise<UploadStatusResponse>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend list endpoint, stats endpoint, and processing_time</name>
  <files>backend/app/schemas/upload.py, backend/app/api/uploads.py, backend/tests/test_uploads.py</files>
  <behavior>
    - Test 1: GET /api/uploads returns 200 with items array, total, page, per_page, pages fields
    - Test 2: GET /api/uploads?page=1&per_page=10 returns correct pagination (offset/limit)
    - Test 3: GET /api/uploads?search=test returns only uploads whose filename contains "test" (case-insensitive)
    - Test 4: GET /api/uploads?verdict=REAL returns only uploads with verdict=REAL
    - Test 5: GET /api/uploads?verdict=failed returns only uploads with status=failed
    - Test 6: GET /api/uploads/stats returns 200 with total, real, fake counts
    - Test 7: GET /api/uploads/{id}/status includes processing_time field (computed from completed_at - created_at)
  </behavior>
  <action>
    1. Add to backend/app/schemas/upload.py:
       - Add `processing_time: float | None = None` field to UploadStatusResponse
       - Add `completed_at: str | None = None` field to UploadStatusResponse
       - Create UploadListResponse schema: items (list[UploadStatusResponse]), total (int), page (int), per_page (int), pages (int)
       - Create StatsResponse schema: total (int), real (int), fake (int)

    2. Add to backend/app/api/uploads.py:
       - Create helper function `_to_status_response(upload: Upload) -> UploadStatusResponse` that computes processing_time as `(upload.completed_at - upload.created_at).total_seconds()` when both exist, else None. Use this in both the existing get_upload_status endpoint and the new list endpoint.
       - Create `GET /api/uploads/stats` endpoint (register BEFORE the `/{upload_id}/status` route to avoid path conflict). Query: count total, count where verdict=REAL, count where verdict=FAKE. Return StatsResponse.
       - Create `GET /api/uploads` (the "" path on the router) endpoint accepting query params: page (int, default=1), per_page (int, default=10), search (str | None), verdict (str | None). Query Upload table ordered by created_at DESC. If search is provided, filter by filename ilike. If verdict is provided and equals "failed", filter by status=="failed"; otherwise filter by verdict==verdict.upper(). Count total matching records. Apply offset=(page-1)*per_page and limit=per_page. Return UploadListResponse.
       - IMPORTANT: The stats route must be registered before the `/{upload_id}/status` route, otherwise FastAPI will try to parse "stats" as a UUID. Move or reorder routes so `GET /uploads/stats` comes before `GET /uploads/{upload_id}/status`.
       - Update existing get_upload_status to use _to_status_response helper.

    3. Write tests in backend/tests/test_uploads.py:
       - Add a `list_client` fixture that creates a mock session returning a list of mock uploads (use make_mock_upload). Mock session.execute to return appropriate results for select queries. Use the same lifespan/ASGITransport pattern as existing fixtures.
       - Add a `stats_client` fixture similarly.
       - Write the 7 tests from the behavior block above.
       - For mock query results: mock db.execute() to return a MagicMock with .scalar() for counts and .scalars().all() for lists.

    4. Update frontend/src/lib/api.ts:
       - Add `processing_time: number | null` and `completed_at: string | null` to UploadStatusResponse interface
       - Add UploadListResponse interface: { items: UploadStatusResponse[]; total: number; page: number; per_page: number; pages: number }
       - Add StatsResponse interface: { total: number; real: number; fake: number }
       - Add `getUploads(params: { page?: number; per_page?: number; search?: string; verdict?: string })` function that builds URLSearchParams and fetches GET /api/uploads
       - Add `getStats()` function that fetches GET /api/uploads/stats
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_uploads.py -x -q</automated>
  </verify>
  <done>GET /api/uploads returns paginated list with search/filter, GET /api/uploads/stats returns counts, processing_time computed in responses, all tests pass, frontend API types updated</done>
</task>

<task type="auto">
  <name>Task 2: Results page multi-card refactor and HomePage quick stats</name>
  <files>frontend/src/pages/ResultsPage.tsx, frontend/src/pages/HomePage.tsx</files>
  <action>
    Refactor ResultsPage.tsx from a single card into a multi-card layout per user decision. Keep all existing fields (verdict badge, confidence bar, visual/audio scores, SHA-256 hash, speech detection info). The page structure for completed analyses becomes:

    1. **Verdict Card** (top): Contains the verdict badge (REAL in green-100/green-700, FAKE in red-100/red-700) and the confidence bar (green-500 for REAL, red-500 for FAKE) with percentage. Same visual style as current but in its own rounded-lg border card.

    2. **Modality Breakdown Card** (middle): Contains the 2-column grid with visual score card (Eye icon, blue-500) and audio score card (Ear icon, purple-500) -- same as current. Add below the grid: a paragraph with the static decision basis text "Prototype model -- verdict is based on untrained fusion weights" in italic text-zinc-500 text-sm.

    3. **Metadata Card** (bottom): Contains filename, submission date (from created_at), processing time (from processing_time field -- display as "Xs" or "N/A" if null), and SHA-256 hash (full hash in mono font with break-all, same style as current).

    4. **Post-result actions** (below cards): A flex row with 3 buttons:
       - "Upload another video" -- Link to /upload, styled as zinc-900 bg button (consistent with HomePage CTA)
       - "Share results link" -- button that calls `navigator.clipboard.writeText(window.location.href)`, then changes button text to "Copied!" for 2 seconds using useState. Wrap in try/catch.
       - "Download PDF" -- button placeholder (disabled with title="Coming soon"), will be wired in Plan 02. Use a FileDown icon from lucide-react.

    Keep all existing non-completed states unchanged (loading, error, in-progress status display with STATUS_LABELS).

    For HomePage.tsx, replace the placeholder content:
    1. Fetch stats from `/api/uploads/stats` on mount using useEffect + useState. Import getStats from lib/api.ts.
    2. Show the existing hero text (TrustChain-AV title and description).
    3. Below the description, add a stats section with 3 metric cards in a grid (grid-cols-3 on md, grid-cols-1 on mobile):
       - "Total Analyses" card showing stats.total count
       - "Detected Real" card showing stats.real count (green-500 text for the number)
       - "Detected Fake" card showing stats.fake count (red-500 text for the number)
       Each card: rounded-lg border border-zinc-200 bg-white p-4 text-center, label in text-xs uppercase text-zinc-400, value in text-2xl font-bold text-zinc-900 (or colored as noted).
    4. Below stats, two CTA buttons in a flex row: "Upload Video" (Link to /upload, zinc-900 bg) and "View History" (Link to /history, border border-zinc-300 bg-white text-zinc-700 hover:bg-zinc-50).
    5. If stats fetch fails or is loading, show the cards with "--" as placeholder values.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>ResultsPage shows 3 separate cards (verdict, modality, metadata) with decision basis text and post-result actions for completed analyses. HomePage shows quick stats fetched from backend with navigation CTAs. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
1. `cd backend && python -m pytest tests/ -v` -- all backend tests pass (existing + new list/stats tests)
2. `cd frontend && npx tsc --noEmit` -- frontend compiles without type errors
3. Manual: Visit /results/{id} for a completed analysis -- verify 3-card layout with verdict, modality breakdown + decision basis, metadata + processing time
4. Manual: Visit / -- verify quick stats cards show correct counts, CTA buttons link to /upload and /history
5. Manual: Click "Share results link" on results page -- verify URL copied and button text changes to "Copied!"
</verification>

<success_criteria>
- Backend serves GET /api/uploads with pagination, search, filter -- returns correct UploadListResponse
- Backend serves GET /api/uploads/stats -- returns total, real, fake counts
- processing_time computed and included in all upload response endpoints
- ResultsPage displays multi-card layout: verdict card, modality card (with decision basis text), metadata card (with processing time)
- Post-result actions present: "Upload another video" navigates to /upload, "Share results link" copies URL, "Download PDF" placeholder visible
- HomePage shows stats fetched from backend and navigation CTAs
- All backend tests pass, frontend TypeScript compiles
</success_criteria>

<output>
After completion, create `.planning/phases/04-results-ui-and-frontend-integration/04-01-SUMMARY.md`
</output>
