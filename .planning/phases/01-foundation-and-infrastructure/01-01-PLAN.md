---
phase: 01-foundation-and-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/requirements.txt
  - backend/app/__init__.py
  - backend/app/main.py
  - backend/app/config.py
  - backend/app/database.py
  - backend/app/api/__init__.py
  - backend/app/api/router.py
  - backend/app/api/health.py
  - backend/app/models/__init__.py
  - backend/app/models/base.py
  - backend/app/models/upload.py
  - backend/app/models/blockchain.py
  - backend/app/services/__init__.py
  - backend/app/ml/__init__.py
  - backend/app/ml/loader.py
  - backend/alembic.ini
  - backend/alembic/env.py
  - backend/alembic/script.py.mako
  - backend/tests/__init__.py
  - backend/tests/conftest.py
  - backend/tests/test_health.py
  - backend/tests/test_database.py
  - backend/pyproject.toml
autonomous: true
requirements:
  - INFR-01
  - INFR-02
  - INFR-04
  - INFR-05

must_haves:
  truths:
    - "FastAPI app starts and serves requests on port 8000"
    - "GET /api/health returns 200 with database and model status"
    - "Database tables (uploads, blockchain_records) exist and accept CRUD operations"
    - "ML model stubs load once at startup via lifespan, visible in health response"
    - "Alembic migrations can create the schema from scratch"
  artifacts:
    - path: "backend/app/main.py"
      provides: "FastAPI app with lifespan model loading"
      exports: ["app"]
    - path: "backend/app/database.py"
      provides: "Async SQLAlchemy engine and session factory"
      exports: ["engine", "async_session", "get_db"]
    - path: "backend/app/models/upload.py"
      provides: "Upload model (merged uploads+results)"
      contains: "class Upload"
    - path: "backend/app/models/blockchain.py"
      provides: "BlockchainRecord model"
      contains: "class BlockchainRecord"
    - path: "backend/app/api/health.py"
      provides: "Health check endpoint"
      contains: "async def health_check"
    - path: "backend/app/ml/loader.py"
      provides: "Model stub loading"
      contains: "def load_model_stubs"
    - path: "backend/app/config.py"
      provides: "Pydantic settings configuration"
      contains: "class Settings"
  key_links:
    - from: "backend/app/main.py"
      to: "backend/app/ml/loader.py"
      via: "lifespan calls load_model_stubs, stores on app.state"
      pattern: "app\\.state\\.(visual_model|audio_model)"
    - from: "backend/app/api/health.py"
      to: "backend/app/database.py"
      via: "health check queries DB with SELECT 1"
      pattern: "session\\.execute.*SELECT 1"
    - from: "backend/app/api/health.py"
      to: "backend/app/main.py"
      via: "reads model status from request.app.state"
      pattern: "request\\.app\\.state"
    - from: "backend/alembic/env.py"
      to: "backend/app/models/base.py"
      via: "target_metadata = Base.metadata"
      pattern: "target_metadata.*Base\\.metadata"
---

<objective>
Create the complete FastAPI backend with async PostgreSQL database, health endpoint, model preloading stubs, and Alembic migrations.

Purpose: This is the backend skeleton that every subsequent phase builds on -- upload handling, ML inference, and blockchain services all plug into this foundation.
Output: A fully structured backend/ directory with working FastAPI app, database models, health endpoint, model stubs, Alembic migrations, and passing tests.
</objective>

<execution_context>
@C:/Users/zopev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/zopev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-infrastructure/01-CONTEXT.md
@.planning/phases/01-foundation-and-infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create backend project structure, config, database, and models</name>
  <files>
    backend/requirements.txt,
    backend/pyproject.toml,
    backend/app/__init__.py,
    backend/app/config.py,
    backend/app/database.py,
    backend/app/models/__init__.py,
    backend/app/models/base.py,
    backend/app/models/upload.py,
    backend/app/models/blockchain.py,
    backend/app/services/__init__.py,
    backend/app/ml/__init__.py,
    backend/app/ml/loader.py
  </files>
  <action>
    Delete any existing `backend/` directory content and start fresh.

    1. Create `backend/requirements.txt` with:
       fastapi[standard], sqlalchemy[asyncio], asyncpg, alembic, pydantic-settings, python-dotenv, httpx, uvicorn[standard], pytest, pytest-asyncio

    2. Create `backend/pyproject.toml` with pytest config: asyncio_mode = "auto"

    3. Create `backend/app/__init__.py` (empty)

    4. Create `backend/app/config.py`:
       - Use pydantic-settings BaseSettings
       - Fields: database_url (default: postgresql+asyncpg://trustchain:trustchain@db:5432/trustchain_db), debug (bool, default False), api_prefix (str, default "/api"), cors_origins (list[str], default ["http://localhost:5173"])
       - model_config with env_file=".env"
       - Export singleton: settings = Settings()

    5. Create `backend/app/database.py`:
       - create_async_engine with settings.database_url, pool_size=5, max_overflow=10, pool_pre_ping=True
       - async_sessionmaker with AsyncSession, expire_on_commit=False
       - async generator get_db() that yields sessions

    6. Create `backend/app/models/base.py`:
       - DeclarativeBase subclass: class Base(DeclarativeBase): pass

    7. Create `backend/app/models/upload.py`:
       - Upload model with __tablename__ = "uploads"
       - Columns: id (UUID, primary_key, default uuid4), filename (String 255), file_path (String 500, nullable), file_hash (String 64, nullable), status (String 20, default "pending"), verdict (String 10, nullable), confidence (Float, nullable), visual_score (Float, nullable), audio_score (Float, nullable), speech_detected (Boolean, nullable), audio_weight (Float, nullable), created_at (DateTime, server_default func.now()), completed_at (DateTime, nullable)
       - Use SQLAlchemy 2.0 Mapped[] annotations with mapped_column()

    8. Create `backend/app/models/blockchain.py`:
       - BlockchainRecord model with __tablename__ = "blockchain_records"
       - Columns: id (UUID, primary_key, default uuid4), upload_id (UUID, ForeignKey to uploads.id), file_hash (String 64), merkle_root (String 66, nullable), tx_hash (String 66, nullable), block_number (Integer, nullable), registered_at (DateTime, nullable), created_at (DateTime, server_default func.now())
       - ForeignKey relationship to Upload

    9. Create `backend/app/models/__init__.py` that imports Upload and BlockchainRecord (ensures Alembic autogenerate finds them)

    10. Create `backend/app/ml/__init__.py` (empty)

    11. Create `backend/app/ml/loader.py`:
        - Function load_model_stubs() that returns dict with visual_model and audio_model entries
        - Each stub is a dict: {"name": "ResNet-50"/"Wav2Vec2", "status": "stub", "loaded": True}
        - Log "Loading ML model stubs..." and "ML model stubs loaded successfully" via logging
        - Do NOT install PyTorch -- stubs are plain dicts

    12. Create `backend/app/services/__init__.py` (empty placeholder)
  </action>
  <verify>
    <automated>cd backend && python -c "from app.config import settings; from app.models import Upload, BlockchainRecord; from app.ml.loader import load_model_stubs; print('imports OK'); stubs = load_model_stubs(); assert stubs['visual_model']['loaded']; print('stubs OK')"</automated>
  </verify>
  <done>All backend Python modules import cleanly, Settings loads defaults, Upload and BlockchainRecord models are defined with correct columns, model stubs return expected structure</done>
</task>

<task type="auto">
  <name>Task 2: Create FastAPI app with lifespan, health endpoint, API router, and Alembic setup</name>
  <files>
    backend/app/main.py,
    backend/app/api/__init__.py,
    backend/app/api/router.py,
    backend/app/api/health.py,
    backend/alembic.ini,
    backend/alembic/env.py,
    backend/alembic/script.py.mako,
    backend/alembic/versions/.gitkeep
  </files>
  <action>
    1. Create `backend/app/api/__init__.py` (empty)

    2. Create `backend/app/api/health.py`:
       - APIRouter with tag "health"
       - GET "/health" endpoint (async def health_check)
       - Check DB connectivity: open async session, execute text("SELECT 1"), catch exceptions -> "unhealthy"
       - Read model status from request.app.state (visual_model, audio_model) using getattr with None default
       - Return JSON: { status: "healthy"|"degraded", database: "healthy"|"unhealthy", models: { visual: status, audio: status }, version: "0.1.0" }

    3. Create `backend/app/api/router.py`:
       - Main APIRouter with prefix "/api"
       - Include health router

    4. Create `backend/app/main.py`:
       - Import asynccontextmanager from contextlib
       - Define lifespan(app) async context manager:
         - On startup: call load_model_stubs(), store visual_model and audio_model on app.state, log messages
         - On shutdown: set models to None, log shutdown
       - Create FastAPI app with title="TrustChain-AV", version="0.1.0", lifespan=lifespan
       - Add CORSMiddleware with settings.cors_origins, allow_credentials=True, allow_methods=["*"], allow_headers=["*"]
       - Include the main API router
       - Do NOT use @app.on_event -- use lifespan only

    5. Initialize Alembic:
       - Create `backend/alembic.ini` with script_location = alembic, sqlalchemy.url placeholder (overridden in env.py)
       - Create `backend/alembic/env.py` using the async template pattern:
         - Import Base from app.models.base
         - Import app.models.upload and app.models.blockchain (so autogenerate sees them)
         - Import settings from app.config
         - Set target_metadata = Base.metadata
         - Override sqlalchemy.url from settings.database_url
         - Implement run_migrations_offline() and run_migrations_online() with async engine
       - Create `backend/alembic/script.py.mako` (standard Alembic template)
       - Create `backend/alembic/versions/.gitkeep`

    6. Create test infrastructure:
       - `backend/tests/__init__.py` (empty)
       - `backend/tests/conftest.py`:
         - Create test FastAPI app using httpx.AsyncClient with ASGITransport
         - Fixture: async_client that provides httpx.AsyncClient pointing at the test app
         - Override get_db dependency if needed (for unit tests, can mock the DB check)
       - `backend/tests/test_health.py`:
         - test_health_returns_200: GET /api/health returns 200
         - test_health_has_model_status: response includes models.visual and models.audio fields
         - test_models_loaded_at_startup: models show "stub" status (not "not_loaded")
       - `backend/tests/test_database.py`:
         - test_upload_model_has_required_columns: verify Upload model has all expected column names
         - test_blockchain_record_model_has_required_columns: verify BlockchainRecord has all expected columns
         - test_models_use_uuid_primary_keys: verify both models use UUID PKs

    Note on tests: test_health tests use httpx.AsyncClient against the real app (no DB needed for model status checks). test_database tests inspect SQLAlchemy model metadata (no live DB needed). This allows tests to pass WITHOUT Docker/PostgreSQL running.
  </action>
  <verify>
    <automated>cd backend && pip install -r requirements.txt -q 2>/dev/null; python -m pytest tests/ -x -q</automated>
  </verify>
  <done>FastAPI app starts with lifespan loading model stubs, GET /api/health returns 200 with model and DB status, Alembic config is in place for future migrations, all tests pass</done>
</task>

</tasks>

<verification>
1. `cd backend && python -m pytest tests/ -x -v` -- all tests green
2. `cd backend && python -c "from app.main import app; print(app.title)"` -- prints "TrustChain-AV"
3. `cd backend && python -c "from app.models.base import Base; print([t for t in Base.metadata.tables])"` -- prints ["uploads", "blockchain_records"]
</verification>

<success_criteria>
- FastAPI app with lifespan-based model stub loading (INFR-01, INFR-05)
- Upload and BlockchainRecord SQLAlchemy models defined with all columns (INFR-02)
- Health endpoint returns 200 with database and model status (INFR-04)
- Alembic configured for async migrations
- All pytest tests pass without requiring a running database
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-infrastructure/01-01-SUMMARY.md`
</output>
