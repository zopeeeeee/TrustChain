---
phase: 01-foundation-and-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/tsconfig.app.json
  - frontend/tsconfig.node.json
  - frontend/index.html
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/index.css
  - frontend/src/lib/utils.ts
  - frontend/src/pages/HomePage.tsx
  - frontend/src/pages/UploadPage.tsx
  - frontend/src/pages/ResultsPage.tsx
  - frontend/src/pages/HistoryPage.tsx
  - frontend/src/components/layout/NavBar.tsx
  - frontend/components.json
autonomous: true
requirements:
  - INFR-01

must_haves:
  truths:
    - "React app renders in the browser at localhost:5173"
    - "Navigation links route between Home, Upload, Results, and History pages"
    - "Each page shell renders its name as heading and placeholder content"
    - "shadcn/ui is initialized and cn() utility works"
  artifacts:
    - path: "frontend/src/App.tsx"
      provides: "React Router setup with all routes"
      contains: "BrowserRouter"
    - path: "frontend/src/pages/HomePage.tsx"
      provides: "Home page shell"
      contains: "HomePage"
    - path: "frontend/src/pages/UploadPage.tsx"
      provides: "Upload page shell"
      contains: "UploadPage"
    - path: "frontend/src/pages/ResultsPage.tsx"
      provides: "Results page shell with :id param"
      contains: "ResultsPage"
    - path: "frontend/src/pages/HistoryPage.tsx"
      provides: "History page shell"
      contains: "HistoryPage"
    - path: "frontend/src/components/layout/NavBar.tsx"
      provides: "Navigation bar with links to all pages"
      contains: "NavBar"
    - path: "frontend/src/lib/utils.ts"
      provides: "cn() class merge utility"
      contains: "export function cn"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/*.tsx"
      via: "React Router Route elements"
      pattern: "<Route.*element.*Page"
    - from: "frontend/src/components/layout/NavBar.tsx"
      to: "frontend/src/App.tsx"
      via: "Link components pointing to route paths"
      pattern: "<Link.*to="
---

<objective>
Create the React/Vite frontend shell with client-side routing, page placeholders, navigation bar, and shadcn/ui initialization.

Purpose: This frontend skeleton provides the page structure that Phase 2-5 will populate with upload forms, results displays, history tables, and blockchain UI.
Output: A working frontend/ directory with React Router, 4 page shells, navigation bar, shadcn/ui ready, and Tailwind CSS configured.
</objective>

<execution_context>
@C:/Users/zopev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/zopev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-infrastructure/01-CONTEXT.md
@.planning/phases/01-foundation-and-infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite/React project with TypeScript and install dependencies</name>
  <files>
    frontend/package.json,
    frontend/vite.config.ts,
    frontend/tsconfig.json,
    frontend/tsconfig.app.json,
    frontend/tsconfig.node.json,
    frontend/index.html,
    frontend/src/main.tsx,
    frontend/src/index.css,
    frontend/src/lib/utils.ts,
    frontend/components.json
  </files>
  <action>
    Delete any existing `Frontend/` directory (note capital F -- the old one). Create fresh `frontend/` (lowercase).

    1. Run `npm create vite@latest frontend -- --template react-ts` to scaffold the project.

    2. In `frontend/`, install dependencies:
       `npm install react-router-dom clsx tailwind-merge`
       `npm install -D tailwindcss @tailwindcss/vite`

    3. Update `frontend/vite.config.ts`:
       - Add @tailwindcss/vite plugin
       - Add path alias: "@" -> "./src" (using path.resolve)
       - Set server.host to "0.0.0.0" and server.port to 5173

    4. Update `frontend/tsconfig.app.json` to include path alias: "@/*" -> ["./src/*"]

    5. Replace `frontend/src/index.css` with Tailwind v4 setup:
       ```css
       @import "tailwindcss";
       ```
       Plus any base body styles (font-family, background color, etc.)

    6. Create `frontend/src/lib/utils.ts`:
       ```typescript
       import { type ClassValue, clsx } from "clsx";
       import { twMerge } from "tailwind-merge";
       export function cn(...inputs: ClassValue[]) {
         return twMerge(clsx(inputs));
       }
       ```

    7. Initialize shadcn/ui: run `npx shadcn@latest init` with defaults (New York style, zinc base color, CSS variables yes). This creates `components.json` and updates CSS.
       If the CLI is interactive and cannot be automated, manually create `frontend/components.json` with the standard shadcn config pointing to src/components/ui and src/lib/utils.

    8. Clean up Vite scaffolding: remove App.css, default Vite logo/assets, default App.tsx content.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -5 && node -e "const pkg = require('./package.json'); console.log('react-router-dom:', !!pkg.dependencies['react-router-dom']); console.log('tailwind-merge:', !!pkg.dependencies['tailwind-merge'])"</automated>
  </verify>
  <done>Vite/React/TypeScript project created with react-router-dom, Tailwind CSS v4, shadcn/ui config, and cn() utility. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create page shells, navigation bar, and React Router setup</name>
  <files>
    frontend/src/App.tsx,
    frontend/src/main.tsx,
    frontend/src/pages/HomePage.tsx,
    frontend/src/pages/UploadPage.tsx,
    frontend/src/pages/ResultsPage.tsx,
    frontend/src/pages/HistoryPage.tsx,
    frontend/src/components/layout/NavBar.tsx
  </files>
  <action>
    1. Create `frontend/src/components/layout/NavBar.tsx`:
       - Functional component with navigation links using react-router-dom Link
       - Links to: "/" (Home), "/upload" (Upload), "/history" (History)
       - App name "TrustChain-AV" as logo/brand text on the left
       - Use Tailwind classes for a clean horizontal nav bar (flex, justify-between, items-center, bg-white border-b shadow-sm, px-6 py-3)
       - Use useLocation() to highlight the active link

    2. Create `frontend/src/pages/HomePage.tsx`:
       - Default export function HomePage
       - Hero section with app name and tagline: "AI-Powered Deepfake Detection with Blockchain Verification"
       - Brief description and a "Get Started" Link button pointing to /upload
       - Use Tailwind for centered layout with padding

    3. Create `frontend/src/pages/UploadPage.tsx`:
       - Default export function UploadPage
       - Heading "Upload Video" and placeholder text "Video upload form will be implemented in Phase 2"
       - Shell structure ready for drag-and-drop zone to be added later

    4. Create `frontend/src/pages/ResultsPage.tsx`:
       - Default export function ResultsPage
       - Import useParams from react-router-dom to read :id parameter
       - Heading "Analysis Results" and placeholder showing the job ID from params
       - Text "Detection results will be displayed here in Phase 4"

    5. Create `frontend/src/pages/HistoryPage.tsx`:
       - Default export function HistoryPage
       - Heading "Analysis History" and placeholder text "History table will be implemented in Phase 4"

    6. Update `frontend/src/App.tsx`:
       - Import BrowserRouter, Routes, Route from react-router-dom
       - Import NavBar and all 4 page components
       - Render BrowserRouter wrapping NavBar + Routes
       - Routes: "/" -> HomePage, "/upload" -> UploadPage, "/results/:id" -> ResultsPage, "/history" -> HistoryPage

    7. Update `frontend/src/main.tsx`:
       - Import index.css
       - Render App inside React.StrictMode to root element

    Follow conventions from CONTEXT.md: PascalCase component files, camelCase functions, 2-space indent for TypeScript.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && echo "TypeScript OK" && node -e "const fs = require('fs'); const files = ['src/App.tsx','src/pages/HomePage.tsx','src/pages/UploadPage.tsx','src/pages/ResultsPage.tsx','src/pages/HistoryPage.tsx','src/components/layout/NavBar.tsx']; files.forEach(f => { if(!fs.existsSync(f)) throw new Error('Missing: '+f); }); console.log('All page files exist')"</automated>
  </verify>
  <done>React Router configured with 4 routes, NavBar shows navigation links, each page renders heading and placeholder content, TypeScript compiles cleanly</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` -- no TypeScript errors
2. `cd frontend && npx vite build` -- production build succeeds
3. All page files exist with correct exports
</verification>

<success_criteria>
- Vite/React/TypeScript project scaffolded from scratch in frontend/
- React Router v7 configured with 4 routes (/, /upload, /results/:id, /history)
- NavBar with working links between all pages
- shadcn/ui initialized with cn() utility ready
- Tailwind CSS v4 configured and working
- TypeScript compiles without errors
- Production build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-infrastructure/01-02-SUMMARY.md`
</output>
